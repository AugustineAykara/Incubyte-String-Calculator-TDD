package org.aykara4;

public class StringCalculator {

    public int add(String numbers) {
        if(numbers.isEmpty()) {
            return 0;
        }
        int num = Integer.parseInt(numbers);
        return num;
    }

}